<%= form_with(model: sent_request_message, local: true) do |form| %>
  <% if sent_request_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(sent_request_message.errors.count, "error") %> prohibited this request_message from being saved:</h2>

      <ul>
      <% sent_request_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :search_key%>
    <%= form.text_field :search_key %>
  </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
