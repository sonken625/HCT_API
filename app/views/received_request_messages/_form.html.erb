<%= form_with(model: received_request_message, local: true) do |form| %>
  <% if received_request_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(received_request_message.errors.count, "error") %> prohibited this received_request_message from being saved:</h2>

      <ul>
      <% received_request_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
