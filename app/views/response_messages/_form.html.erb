<%= form_with(model: [response_message],url:response_messages_path(request_message) , local: true) do |form| %>
  <% if response_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(response_message.errors.count, "error") %> prohibited this response_message from being saved:</h2>

      <ul>
      <% response_message.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :params_string%>
    <%= form.text_field :params_string %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
